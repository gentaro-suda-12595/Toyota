# 単体テスト仕様書

## 1. はじめに

### 1.1. 目的

本書は、共通関数設計書に定義された関数のうち、**SCD1／SCD2としてのBronze本体への反映処理**が設計通りに動作することを検証するためのテスト仕様を定義する。

### 1.2. 適用範囲

対象テーブルへのCDCイベント投入→ジョブ実行→Bronze側結果確認、という一連の手動テストによって、SCD1（44テーブル）・SCD2（4テーブル）それぞれの反映ロジックを検証する。

### 1.3. 実施方法の概要

* Databricksワークスペース上で、CDC連携元に相当する対象テーブル（op_type/op_ts/pos/before/after等の制御列を持つテーブル）に、テストケースごとのCDCレコードを手動でINSERTする
* Databricks WorkflowsのUIから該当ジョブを手動実行（Run now）する
* ジョブ完了後、Bronze対象テーブルをSELECTし、期待結果と一致するかを目視で確認する
* 1テストケースにつき、投入→実行→確認の1サイクルを1件として扱う。複数レコードを同一バッチで反映させるケースは、まとめて投入してから1回実行する


## 2. 検証観点（全体像）

| 分類 | 確認すること |
| --- | --- |
| 共通関数ログ処理 | 各関数の適切な位置でのログ表示が正しく行われること |
| SCD1処理 | 既存キーの上書き、新規キーの挿入、既存キーの物理削除、TRUNCATE時の全行削除が正しく行われること |
| SCD2処理 | 既存current行のクローズと新版挿入、新規キーの挿入、TRUNCATE時の全current行クローズが正しく行われること |
| 異常系（共通） | 存在しないキーへのD、必須キーの欠落等、過去に確認された不具合パターンが再発しないこと |

## 3. テストケース

各ケースは「前提（既存Bronze状態）」「投入するCDCレコード」「ジョブ実行」「期待結果（Bronze側で確認する内容）」の順に確認する。時刻は東京基準NTZ、秒単位で表記する。


### 3.1. SCD1（対象テーブル：）

| No | 検証観点 | 投入するCDCレコード | 期待結果 |
| --- | --- | --- | --- |
| SCD1-01 | 新規キーの挿入(新規) | I | レコードが挿入される |
| SCD1-02 | 新規キーの挿入(既存) | I | なし |
| SCD1-03 | 新規キーの挿入後、同一主キーを新規挿入 | I → I |  |
| SCD1-04 | 新規キーの挿入後、同一主キーを更新 | I → U | レコードが挿入され、同一主キーのレコードが上書きされる |
| SCD1-05 | 新規キーの挿入後、同一主キーを物理削除 | I → D | レコードが挿入され、同一主キーのレコードが物理削除される |
| SCD1-06 | 既存キーの更新(新規) | U | なし |
| SCD1-07 | 既存キーの更新(既存) | U | レコードが上書きされる |
| SCD1-08 | 既存キーの更新後、同一主キーを新規挿入 | U → I |  |
| SCD1-09 | 既存キーの更新後、同一主キーを更新 | U → U | 2回上書きされる |
| SCD1-10 | 既存キーの更新後、同一主キーを物理削除 | U → D | レコードが上書きされ、同一主キーのレコードが物理削除される |
| SCD1-11 | 既存キーの物理削除(新規) | D | なし |
| SCD1-12 | 既存キーの物理削除(既存) | D | レコードが物理削除される |
| SCD1-13 | 既存キーの物理削除後、同一主キーを挿入 | D → I | レコードが物理削除され、同一主キーのレコードが挿入される |
| SCD1-14 | 既存キーの物理削除後、同一主キーを更新 | D → U | 　|
| SCD1-15 | 既存キーの物理削除後、同一主キーを物理削除 | D → D |  |
| SCD1-16 | TRUNCATEによる全行削除 | T | 対象テーブルの全行が削除される |
| SCD1-17 | TRUNCATEと同一主キーの新規挿入 | T → I | なし |
| SCD1-18 | TRUNCATEと同一主キーの新規挿入 | T → U | なし |
| SCD1-19 | TRUNCATEと同一主キーの新規挿入 | T → D | なし |


### 3.2. SCD2（対象テーブル：）

| No | 検証観点 | 投入するCDCレコード | 期待結果 |
| --- | --- | --- | --- |
| SCD2-01 | 新規キーの挿入(新規) | I | レコードが挿入される |
| SCD2-02 | 新規キーの挿入(既存) | I | なし |
| SCD2-03 | 新規キーの挿入後、同一主キーを新規挿入 | I → I |  |
| SCD2-04 | 新規キーの挿入後、同一主キーを更新 | I → U | レコードが挿入され、同一主キーのレコードが上書きされる |
| SCD2-05 | 新規キーの挿入後、同一主キーを物理削除 | I → D | レコードが挿入され、同一主キーのレコードが物理削除される |
| SCD2-06 | 既存キーの更新(新規) | U | なしる |
| SCD2-07 | 既存キーの更新(既存) | U | レコードが上書きされる |
| SCD2-08 | 既存キーの更新後、同一主キーを新規挿入 | U → I |  |
| SCD2-09 | 既存キーの更新後、同一主キーを更新 | U → U | 2回上書きされる |
| SCD2-10 | 既存キーの更新後、同一主キーを物理削除 | U → D | レコードが上書きされ、同一主キーのレコードが物理削除される |
| SCD2-11 | 既存キーの物理削除(新規) | D | なし |
| SCD2-12 | 既存キーの物理削除(既存) | D | レコードが物理削除される |
| SCD2-13 | 既存キーの物理削除後、同一主キーを挿入 | D → I | レコードが物理削除され、同一主キーのレコードが挿入される |
| SCD2-14 | 既存キーの物理削除後、同一主キーを更新 | D → U | 　|
| SCD2-15 | 既存キーの物理削除後、同一主キーを物理削除 | D → D | レコードが物理削除され、2回目の処理はスルー |
| SCD2-16 | TRUNCATEによる全行削除 | T | 対象テーブルの全行が削除される |
| SCD2-17 | TRUNCATEと同一主キーの新規挿入 | T → I | なしる |
| SCD2-18 | TRUNCATEと同一主キーの新規挿入 | T → U | なし |
| SCD2-19 | TRUNCATEと同一主キーの新規挿入 | T → D | なし |

### 3.3. 共通関数のログ

| No | 関数名 | 検証観点 | 期待結果 |
| --- | --- | --- | --- |
| 1 | process_microbatch | マイクロバッチの開始(同時に処理対象のCDCを表示) | ログに「どういうエラーが」表示がされている |
| 2 | process_microbatch | マイクロバッチの完了 | ログに「どういうエラーが」表示がされている |
| 3 | merge_scd1_records | SCD1の処理開始 | ログに「どういうエラーが」表示がされている |
| 4 | merge_scd1_records | パーティション分割完了 | ログに「どういうエラーが」表示がされている |
| 5 | merge_scd1_records | SCD1のマージ処理完了 | ログに「どういうエラーが」表示がされている |
| 6 | merge_scd2_history | SCD2の処理開始 | ログに「どういうエラーが」表示がされている |
| 7 | merge_scd2_history | パーティション分割完了 | ログに「どういうエラーが」表示がされている |
| 8 | merge_scd2_history | SCD2のマージ処理完了 | ログに「どういうエラーが」表示がされている |
| 9 | execute_truncate | トランケート開始 | ログに「どういうエラーが」表示がされている |
| 10 | execute_truncate | トランケート完了 | ログに「どういうエラーが」表示がされている |

### 3.4. 監視ログに基づくエラー検証

| No | 検証観点 | 投入するCDCレコード | 期待結果 |
| --- | --- | --- | --- |
| Neg-01 | CDC標準キー（table）が欠落 |  | エラーメッセージNo.1が表示される |
| Neg-02 | CDC標準キー（pos）が欠落 |  | エラーメッセージNo.1が表示される |
| Neg-03 | CDC標準キー（op_type）が欠落 | 欠落 | エラーメッセージNo.1が表示される |
| Neg-04 | CDC標準キー（op_ts）が欠落 |  | エラーメッセージNo.1が表示される |
| Neg-05 | CDC標準キー（current_ts）が欠落 |  | エラーメッセージNo.1が表示される |
| Neg-06 | ソース側の主キーが欠落 |  | エラーメッセージNo.2が表示される |
| Neg-07 | op_type＝"U"の場合にBeforeが欠落 | U | エラーメッセージNo.3が表示される |
| Neg-08 | op_type＝"D"の場合にBeforeが欠落 | D | エラーメッセージNo.3が表示される |
| Neg-09 | op_type＝"I"の場合にAfterが欠落 | I | エラーメッセージNo.3が表示される |
| Neg-10 | op_type＝"U"の場合にAfterが欠落 | U | エラーメッセージNo.3が表示される |
| Neg-11 | op_typeがI,U,D,T以外の値である | I,U,D,T以外の値 | エラーメッセージNo.4が表示される |
| Neg-12 | CDCデータの取得失敗 |  | エラーメッセージNo.5が表示される |


#### 3.4.1 監視ログに基づくエラーメッセージ

| No | エラーメッセージ内容 |
| --- | ---|
| 1 | Mandatory keys are missing. Target table: [対象テーブルパス] CDC data: [1レコード分のCDCデータをそのまま記載] |
| 2 | Primary keys are missing. Target table: [対象テーブルパス] CDC data: [1レコード分のCDCデータをそのまま記載] |
| 3 | History Data are missing. Target table: [対象テーブルパス] CDC data: [1レコード分のCDCデータをそのまま記載] |
| 4 | Invalid op_type value detected. Target table: [対象テーブルパス] CDC data: [1レコード分のCDCデータをそのまま記載] |
| 5 | Data source cannot be confirmed. Target table: [対象テーブルパス] |



## 4. 判定基準

各テストケースについて、「期待結果」列の内容とBronze側の実データが完全一致する場合を合格（OK）とする。1件でも一致しない場合は不合格（NG）とし、差異内容を記録する（5章の結果報告書に記載）。
