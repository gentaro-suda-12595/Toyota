# 要員管理・スキルマップシステム 

---

## 1. 余剰要員抽出機能 (`surplusPersonnelExtraction`)

### 概要・役割
「要員計画 2026」シートから、区分が「社員」かつ当月以降の合計稼働率が100%未満（または「学習組」）の余剰要員（空き要員）を全自動で抽出し、シート上部（7行目〜）に挿入・表示する機能。

### 処理フロー
1. **既存データの確認・備考退避**: 7行目以降にある前回の抽出結果を確認し、**要員名と備考（AE列）をメモリ（Map）に保持**してから既存行を削除。
2. **当月位置の判定**: `getWorkloadMonthIndex()` を呼び出し、実行時の月（4月〜翌3月）に対応する列（Q列〜AB列）のインデックスを取得。
3. **社員データのフィルタリング＆合算**: 区分が「社員」の行のみを対象に、当月以降のアクティブな稼働率を算出して余剰判定を実施。
4. **結果の出力・備考復元**: 抽出結果を行挿入して書き込み、**退避しておいた前回の備考欄を名前で突合して再セット**。
5. **後続処理の実行**: 罫線適用（`applyBordersForSurplusStaff`）、背景色更新（`colorByWorkloads`）、要員数出力（`putNumberOfSurplusPersonnel`）を実行。

### フローチャート
```text
[開始]
  ↓
[既存の余剰要員行を取得 & 備考(AE列)をMapに保持]
  ↓
[既存の余剰要員行を削除]
  ↓
[実行時の当月インデックス(Q列〜AB列)を判定]
  ↓
[元データをループ (区分="社員" のみ判定)]
  ├─ 当月以降の稼働率を合算
  └─ 100%未満 または "学習組" を抽出対象リストへ追加
  ↓
[抽出結果を7行目に新規行挿入して出力]
  ↓
[Mapに保持していた備考を名前で突合して復元]
  ↓
[罫線適用・稼働率色付け関数を呼び出し]
  ↓
[終了]
```

### 各関数の役割と説明
* `surplusPersonnelExtraction`: 余剰要員抽出のメイン処理関数。既存行の退避・削除から、データの絞り込み、新規行挿入、備考復元までのメインロジックを制御。
* `applyBordersForSurplusStaff`: 抽出・挿入された余剰要員の一覧範囲に対し、指定されたスタイル（実線・点線・枠線）の罫線を一括適用。
* `getWorkloadMonthIndex`: 現在の日付から「4月〜翌年3月」の12ヶ月サイクル内における当月の列インデックス（0〜11）を動的に算出。

### 重要ポイント（システム上の根幹）
* 備考欄（AE列）のデータ保持仕組み
* 再抽出時に行を削除・再生成するため、そのままでは手入力された備考が消失します。`removeSpaces(name)` で氏名の空白を除去（名寄せ）した状態で `savedRemarksMap` に保存し、出力後に突合して復元することでデータの保持を実現。
<br>

## 2. 稼働率・工数セル自動着色機能 (`colorByWorkloads`)

### 概要・役割
P列〜AB列（月別稼働率）に入力されている数値や形式（%表記か工数数値か）を判別し、一括でセル背景色を着色する機能。

### 処理フロー
1. **1回目のループ（合算値の計算）**: 氏名×月ごとの合計稼働率（%表記のみ）を `sumMap` に集計。
※上部に挿入された「余剰要員リスト」は二重計算になるためスキップし、「元データ」部分のみを合算。
2. **2回目のループ（着色判定）**: セルの値に応じて背景色配列（bgColors）を生成。
 * 100%以上: 薄い緑 (#D9FDD3)
 * 100%未満 (0〜99%): オレンジ (#FFA500)
 * 工数数値 (0.5等): 薄いピンク (#fbe4d5)
 * 未入力/テキスト ("休職"等): 色なし (null)
3. **一括適用**: `setBackgrounds(bgColors)` で一括処理。

### フローチャート
```text
[開始]
  ↓
[1回目のループ: 元データ行のみを対象に氏名×月ごとの稼働率(%)を合算 (sumMap)]
  ↓
[2回目のループ: 全セルに対して着色条件を判定]
  ├─ %表記 & 合計100%以上 → 薄い緑 (#D9FDD3)
  ├─ %表記 & 合計100%未満 → オレンジ (#FFA500)
  ├─ %なしの純粋な数値   → 薄いピンク (#fbe4d5)
  └─ 空欄・文字 ("終了"等) → 色なし
  ↓
[setBackgrounds でシートに一括着色]
  ↓
[終了]
```

### 重要ポイント（システム上の根幹）
**二重計算防止フラグ (`isOriginalList`)**:
上部に「余剰要員リスト」が挿入されている状態で集計を行うと、同じ人物の数値が2重に加算されて誤った合計値になっている。氏名が空欄になる境界線を検知して `isOriginalList = true` に切り替えることで、元データ行のみを正確に合算。
<br>
## スキルマップ（全体）集計機能 (`aggregateSkillMap`)

### 概要・役割
全要員の保有スキル（言語・DB・環境・工程）を集計し、保有人数の多い順（降順）に整列した割合表を「スキルマップ（全体）」シートに出力する機能。

### 処理フロー
1. **データ読み込み＆分割**: 各セル内のスキル文字列を「、」「,」「スペース」で分割し、大文字に統一（toUpperCase()）してカウント。
2. **ソート＆割合算出**: カウント数の多い順にソートし、全体の合計数に対する「割合（%）」を算出。
3. **2次元配列の構築**: 【言語】【DB】【環境】【工程】の4ブロックを横並び（間に1列の空白）にした出力用配列を作成。
4. **描画＆罫線適用**: シートをクリア後に書き込み、パーセントフォーマット設定、および `applyBordersForSkillMap` でヘッダー装飾（グレー背景）と外枠実線・内側点線枠を適用。

### フローチャート
```text
[開始]
  ↓
[元データのスキル列をスキャン]
  ↓
[文字列をカンマや読点で分割し、大文字統一して集計 (countSkills)]
  ↓
[カテゴリごとに件数が多い順に降順ソート & 割合を計算]
  ↓
[4カテゴリ横並びの2次元配列(`outputData`)を作成]
  ↓
[シートをクリアし、2行目・B列から出力]
  ↓
[割合列に "0.0%" フォーマット適用 & Meiryo/11pt設定]
  ↓
[`applyBordersForSkillMap` で罫線と装飾を適用]
  ↓
[終了]
```
<br>

## 4. 個人スキルマップ構築 ＆ 複合検索機能(`createIndividualSkillMapAndRatio` / `executeSkillSearch`)

### 概要・役割
1. **createIndividualSkillMapAndRatio**: 社員のスキルを個人別に一覧化すると同時に、上部に検索用チェックボックス一覧を動的生成する機能。
2. **executeSkillSearch**: チェックのついたスキルで「複合AND検索」を行い、該当する社員のみを下部表に即時表示する機能。

### 処理フロー
【個人スキルマップ＆チェックボックス生成】
1. **スキル抽出**: 社員ごとのスキル構造体（Set構造）を生成しつつ、全スキル一覧を取得。
2. **開始行の動的計算**: スキル数が最も多いカテゴリの件数（`maxSkillCount`）を取得し、表の開始行 `START_ROW = (maxSkillCount + 4) `を決定。
3. **全体クリア**: データ範囲が狭まった場合でもチェックボックスが残らないよう、`getRange(1, 1, maxRows, maxCols).clearDataValidations()` で全域クリア。
4. **レイアウト描画**: 上部にチェックボックス、`START_ROW `から下に「個人表」と「割合表」を描画。

【複合AND検索実行】
1. **START_ROWの動的検知**: B列から「氏名」というヘッダー位置を検索し、現在の表の開始行を特定。
2. **選択スキルの取得**: チェックボックスエリア（3行目 〜 `START_ROW`の2行上）でチェックされたスキルを取得。
3. **AND条件フィルタリング**: 社員の保有スキルすべてに選択スキルが含まれているか判定。
4. **表の書き換え**: リセット後に該当要員のみを再描画（選択なしの場合は全要員表示）。

### フローチャート
```test
【createIndividualSkillMapAndRatio (生成)】
[開始]
  ↓
[社員ごとのスキル(Set) と スキル毎の件数を集計]
  ↓
[最大のスキル種類数から 下部表の開始行(START_ROW) を自動計算]
  ↓
[outputSheet 全域のデータ入力規則(チェックボックス)を完全消去]
  ↓
[3行目〜 にチェックボックス群を配置]
  ↓
[START_ROW行目〜 に個人スキルマップ表 & 割合表を出力・罫線適用]
  ↓
[終了]

【executeSkillSearch (検索ボタン押下時)】
[開始]
  ↓
[B列の「氏名」セルを探し、現在の START_ROW を動的に取得]
  ↓
[チェックが入っているスキル名を配列 (selectedSkills) に格納]
  ↓
[全社員のスキルと照合 (選択スキルが「すべて」含まれるか: AND検索)]
  ↓
[START_ROW以降の個人表エリアをクリア]
  ↓
[該当者のみを再描画 (0名の場合は警告メッセージを表示)]
  ↓
[終了]
```

### 重要ポイント（システム上の根幹）
* **表開始位置の完全自動計算（レイアウト崩れ防止）**:
スキル数がいくら増減しても表が被らないよう、`maxSkillCount + 4` で動的に `START_ROW` を決定しています。また、検索時もB列の「氏名」の位置を検索して `START_ROW` を特定するため、スキル数が増えてもプログラムコードを修正する必要なし。

* **チェックボックスの残存バグ回避**:
単なる `getDataRange()` のクリアでは、データが減ってシートが小さくなった際に範囲外の古いチェックボックスが消えずに残る問題が発生。`outputSheet.getRange(1, 1, maxSheetRows, maxSheetCols).clearDataValidations()` を使うことで、シートの最大サイズ全体から安全かつ確実に古いチェックボックスを消去。
<br>

## 5. 各関数の一覧と役割まとめ（クイックリファレンス）

| 関数名 | 役割・説明 | 
| :--- | :---: | 
| `surplusPersonnelExtraction` | 余剰要員を抽出し、上部に割り込ませて挿入。前回備考欄（AE列）を保持・復元する。 | 
| `applyBordersForSurplusStaff` | 抽出された余剰要員表に対し、指定ルール（デフォルト35行分、超えた場合は拡張）で罫線を設定する。 | 
| `colorByWorkloads` | P列〜AB列の稼働率・工数セルを判定し、緑・オレンジ・ピンクへ一括着色する（二重計算防止機能付き）。 |
| `aggregateSkillMap` | スキル全体マップを集計し、カテゴリ別・件数順にソートした割合表を作成する。|
| `applyBordersForSkillMap` | スキルマップ（全体）の各表にヘッダー装飾（グレー背景）と罫線（実線・点線）を適用する。 |
| `createIndividualSkillMapAndRatio` | 個人スキル表・割合表・および動的チェックボックスを自動位置計算で生成・配置する。 |
| `executeSkillSearch` | チェックされたスキルの複合AND検索を行い、該当する社員のみを下部表に即時表示する。 |
| `normalizeName` / `normalizeNameForSkillMap` | 氏名から空白や注記（※新入社員※など）を除去し、正しく突合するための名寄せ関数。 |
| `parseRate` | 「80%」「0.8」などの数値・文字列表記を、計算可能な小数数値（0.8）に統一変換する関数。 |
| `addSkillsToSet` | 文字列を「、」「,」「スペース」で分解し、大文字統一（toUpperCase）して Set に追加する関数。 |
